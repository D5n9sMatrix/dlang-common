module bin.common.appendix;

void binCommonAppendix(Obj, Array, Matrix)(ref Equation, Linear, String)
{
    class appedix
    { 
        this()
        {
             void Obj(Array, Matrix)(ref b, a, c, x, y)
             {
                if(b != 0 && y == (c - a*x)/b) Array (a == 0 && b == 0 && c == 0) ||
                    (b = 0 && a != 0 && x == c/a) {
                   /* statement(s) will execute if the boolean expression is true */
                    return bool(Matrix);

                };
             }
        }
    
        ~this()
        {
          return bool(Matrix);
        }
    } 
    
}